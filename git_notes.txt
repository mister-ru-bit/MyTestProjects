git init

git config --global user.name "Ruslan Bityukov"

есть еще --local

git config --global user.email "mister_ru@ukr.net"

git status

git add git_notes.txt

git add --all

.gitignore   файл дл€ списка игнора

git commit -m "комментарий"

создание ссылки на удаленный репозиторий
git remote add testproj https://github.com/mister-ru-bit/MyTestProjects

—писок удаленных репозиториев
git remote

заливка проекта на удаленный репозиторий
git push terstproj master

создание новой версии
git tag "version 1.0"

git log  ctrl + c выйти

создание новой ветки
git branch <name>
переход на новую ветку
git checkout <name>

возвращение в master
git checkout master
коммит изменений в master
git commit -m "Fixed errors"

сли€ние веток
git merge

посмотреть какие есть ветки
git branch -a

===============================================
Еor create a new repository on the command line
echo "# MyTestProjects" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/mister-ru1966/MyTestProjects.git
git push -u origin master

Еor push an existing repository from the command line
git remote add origin https://github.com/mister-ru1966/MyTestProjects.git
git branch -M master
git push -u origin master
===============================================